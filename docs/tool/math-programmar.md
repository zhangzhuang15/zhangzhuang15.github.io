---
title: "mathjax语法"
page: true
aside: true
---

## 希腊字母
`$\theta$` $\theta$

`$\Theta$` $\Theta$

`$\gamma$` $\gamma$

`$\Gamma$` $\Gamma$

`$\beta$` $\beta$

`$\alpha$` $\alpha$

`$\epsilon$` $\epsilon$

`$\sigma$` $\sigma$

`$\Sigma$` $\Sigma$

`$\pi$` $\pi$

`$\omega$` $\omega$

`$\Omega$` $\Omega$

`$\mu$` $\mu$

`$\rho$` $\rho$

`$\tau$` $\tau$

`$\chi$` $\chi$

`$\delta$` $\delta$

`$\Delta$` $\Delta$

`$\nabla$` $\nabla$

`$\lambda$` $\lambda$

`$\Lambda$` $\Lambda$

`$\xi$` $\xi$

`$\phi$` $\phi$

`$\Phi$` $\Phi$

`$\psi$` $\psi$

`$\Psi$` $\Psi$

`$\eta$` $\eta$

`$\nu$` $\nu$

`$\zeta$` $\zeta$



## 数学符号 
`$\times$` $\times$

`$\div$` $\div$

`$\pm$` $\pm$

`$\cdot$` $\cdot$

`$\neq$` $\neq$

`$\equiv$` $\equiv$

`$\leq$` $\leq$

`$\geq$` $\geq$

`$\forall$` $\forall$

`$\exists$` $\exists$

`$\because$` $\because$

`$\cup$` $\cup$

`$\cap$` $\cap$

`$\subset$` $\subset$

`$\supset$` $\supset$

`$\subseteq$` $\subseteq$

`$\emptyset$` $\emptyset$

`$\angle ABC$` $\angle ABC$

`$\therefore$` $\therefore$

`$AB \bot CD$` $AB \bot CD$

`$AB \parallel CD$` $AB \parallel CD$

`$\triangle ABC \cong \triangle DEF$` $\triangle ABC \cong \triangle DEF$

`$\triangle ABC \not\cong \triangle DEF$` $\triangle ABC \not\cong \triangle DEF$

`$\triangle ABC \sim \triangle DEF$` $\triangle ABC \sim \triangle DEF$

`$\triangle ABC \not\sim \triangle DEF$` $\triangle ABC \not\sim \triangle DEF$

`$\square ABCD$` $\square ABCD$

`$\odot C$` $\odot C$

`$\overset{\frown}{AB}$` $\overset{\frown}{AB}$

`$\frac{4}{5}$` $\frac{4}{5}$

`$x^2$` $x^2$

`$\sqrt{2}$` $\sqrt{2}$

`$\sqrt[3]{5}$` $\sqrt[3]{5}$

`$\vec{a}$` $\vec{a}$

`$\overrightarrow{BC}$` $\overrightarrow{BC}$

`$\dot{10}$` $\dot{10}$

`$\overline{10}$` $\overline{10}$

`$\overbrace{a...b}$` $\overbrace{a...b}$

`$\overleftarrow{AB}$` $\overleftarrow{AB}$

`$\overleftrightarrow{ab}$` $\overleftrightarrow{ab}$

`$a \in (0,4)$` $a \in (0,4)$

`$a \notin (0,4)$` $a \notin (0,4)$

`$\int_0^1 f(x)dx$` $\int_0^1 f(x)dx$

`$\iint_D f(x,y)dxdy$` $\iint_D f(x,y)dxdy$

`$\iiint_{\Omega} f(x,y,z)dxdydz$` $\iiint_{\Omega} f(x,y,z)dxdydz$

`$\oplus$` $\oplus$

`$\ominus$` $\ominus$

`$\otimes$` $\otimes$

`$\oslash$` $\oslash$

`$25\degree$` $25\degree$

`$\infty$` $\infty$

`$\sum_{i=0}^{100} x^i$` $\sum_{i=0}^{100} x^i$

`$\prod_{i=1}^{10} a_i$` $\prod_{i=1}^{10} a_i$

`$\frac{\partial^2f}{\partial{x}\partial{y}}$` $\frac{\partial^2f}{\partial{x}\partial{y}}$

`$\nabla \cdot \boldsymbol{A}$` $\nabla \cdot \boldsymbol{A}$

行列式1
```latex
$$
\begin{vmatrix}
  x_{11} & x_{12} \\
  x_{21} & x_{22}
\end{vmatrix}
$$
```
$$
\begin{vmatrix}
  x_{11} & x_{12} \\
  x_{21} & x_{22}
\end{vmatrix}
$$

行列式2
```latex
$$
\begin{vmatrix}
  x_{11} & x_{12} & \dots & x_{1n} \\
  x_{21} & x_{22} & \dots & x_{2n} \\
  \vdots & \vdots & \ddots & \vdots \\
  x_{n1} & x_{n2} & \dots & x_{nn}
\end{vmatrix}
$$
```
$$
\begin{vmatrix}
  x_{11} & x_{12} & \dots & x_{1n} \\
  x_{21} & x_{22} & \dots & x_{2n} \\
  \vdots & \vdots & \ddots & \vdots \\
  x_{n1} & x_{n2} & \dots & x_{nn}
\end{vmatrix}
$$

矩阵1
```latex
$$
\begin{matrix}
  1 & 2\\
  3 & 4
\end{matrix}
$$
```
$$
\begin{matrix}
  1 & 2\\
  3 & 4
\end{matrix}
$$

矩阵2
```latex
$$
\begin{pmatrix}
  1 & 2\\
  3 & 4
\end{pmatrix}
$$
```
$$
\begin{pmatrix}
  1 & 2\\
  3 & 4
\end{pmatrix}
$$

矩阵3
```latex 
$$
\begin{bmatrix}
  1 & 2\\
  3 & 4
\end{bmatrix}
$$
```
$$
\begin{bmatrix}
  1 & 2\\
  3 & 4
\end{bmatrix}
$$

矩阵4
```latex 
$$
\begin{Bmatrix}
  1 & 2\\
  3 & 4
\end{Bmatrix}
$$
```
$$
\begin{Bmatrix}
  1 & 2\\
  3 & 4
\end{Bmatrix}
$$

矩阵5
```latex 
$$
\begin{Vmatrix}
  1 & 2\\
  3 & 4
\end{Vmatrix}
$$
```
$$
\begin{Vmatrix}
  1 & 2\\
  3 & 4
\end{Vmatrix}
$$

方程联立1
```latex 
$$
\begin{cases}
  x + y &= 4 \\
  x - y &= 8
\end{cases}
$$
```
$$
\begin{cases}
  x + y &= 4 \\
  x - y &= 8
\end{cases}
$$

方程联立2
```latex 
$$
\begin{cases}
  x &= \rho \times \cos \theta \\
  y &= \rho \times \sin \theta
\end{cases}, \quad \theta \in (0, 2\pi)
$$
```
$$
\begin{cases}
  x &= \rho \times \cos \theta \\
  y &= \rho \times \sin \theta
\end{cases} \quad \theta \in (0, 2\pi)
$$

`$\lim_{x\to 0} e^x = 1$` $\lim_{x\to 0} e^x = 1$

## 数学函数
`$\sin \theta$` $\sin \theta$

`$\cos \theta$` $\cos \theta$

`$\tan \theta$` $\tan \theta$

`$\sec \theta$` $\sec \theta$

`$\cot \theta$` $\cot \theta$

`$\arccos 14$` $\arccos 14$

`$\arcsin 14$` $\arcsin 14$

`$\arctan 14$` $\arctan 14$